{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "mask",
      "skipFiles": [
        "<node_internals>/**"
      ],
      "program": "${workspaceFolder}/mapBounds.tsx"
    },{
      "type": "node",
      "request": "launch",
      "name": "server",
      "skipFiles": [
        "<node_internals>/**"
      ],
      "program": "${workspaceFolder}/server/index.tsx"
    }
  ]
}